# ruby '1.9.3', engine: 'jruby', engine_version: '1.7.9'

gem 'travis-build', github: 'travis-ci/travis-build'

gem 'march_hare'
gem 'multi_json'

# required by travis-build
gem 'addressable'
gem 'active_support'

group :test do
  gem 'rspec'
end
